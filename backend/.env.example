# ============================================
# Application Configuration
# ============================================
NODE_ENV=development
PORT=3000
# Comma-separated list of allowed browser origins (NOT the API URL). Include your frontend URL(s).
# Next.js default: 3001. Vite: 5173. Do not put the backend (e.g. 3000) here.
CORS_ORIGIN=http://localhost:3001,http://localhost:5173

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=course_portal
DB_SSL=false


